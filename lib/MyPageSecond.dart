import 'package:flutter/material.dart';

class MyPageSecond extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return new MaterialApp(
      title: "second",
      home: new FlatButton(onPressed: null, child: new Text("test")),
    );
  }
}
